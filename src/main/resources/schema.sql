CREATE TABLE IF NOT EXISTS student ( id BIGINT PRIMARY KEY AUTO_INCREMENT NOT NULL,document_id VARCHAR(255) NOT NULL,name VARCHAR(255) NOT NULL, lastname VARCHAR(255) NOT NULL, status BOOLEAN NOT NULL, age INT NOT NULL, created_at VARCHAR(255) NOT NULL);